"use client";

import { Input } from "@/components/ui/input";

// add npx shadcn@latest add dropdown-menu

export const UserInteraction = () => {
  return (
    <section className="mt-16 flex w-fit px-[52px]">
      <Input placeholder="Search event" />
    </section>
  );
};
